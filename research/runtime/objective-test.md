# Objective Test (TEST-002/003)

1. Спавн: найти L==10 объект (MISSION_OBJECT_CREATE событие).
2. Атаковать: наблюдать cB уменьшение (источники).
3. Завершить: cB<=0 -> state 7 -> k(n,10) награды (Q[] значения).
