# TEST-003 — REAL RUNTIME MISSION 03 + L==10 + COMPLETION — ОТЧЁТ

**Дата:** 2026-08-16
**Тест:** TEST-003 (Этап 15.32)
**Runtime:** FreeJ2ME SDL (Anbu) x86 JRE 17; JAR SHA-256 `1111f12b...` (не изменён; instrumented.jar побайтово идентичен)
**Вход в mission 03:** RUNTIME_ASSISTED (внешний JDWP putstatic g.bB=3) — вариант B по ТЗ, отмечен как RUNTIME_ASSISTED, не пользовательский проход

## 1. Итог

**PARTIAL** — mission_03 **реально загружена** (state 6, bB=3, by=2, bz=1), L==10 объекты существуют (910), скрипт с opcode 147 найден в g.z. Однако активный mission object **НЕ установлен** (i=0, cB=0): скрипт 147 привязан к тайлу 31, триггер не сработал в FreeJ2ME (путь к тайлу блокирован стенами и диалоговой петлёй у NPC). Completion не достигнут → по ТЗ PASSED невозможен.

## 2. Метод входа (RUNTIME_ASSISTED)

- bB сбрасывается в 0 в C() (state 16, case 2) непосредственно перед state 4 → z() → Z(bB).
- Мониторинг bB каждые 5 мс + восстановление bB=3 сразу после сброса → Z(3) вызывается с правильным значением.
- state 2 sub 1 = **пауза загрузки** (новое наблюдение): продвигается клавишей FIRE (F() case 1: ez!=0 → d(2,0) → g.g()).
- Загрузка: state 16 (sub 1→2→3, ~18 c) → state 4 → state 2 (карты, q до 26) → state 6.

## 3. Подтверждение миссии

| Поле | Значение |
|---|---|
| state | 6 (геймплей) |
| bB | 3 |
| by / bz | 2 / 1 (карта mission_03, header bz=1) |
| resource | /m9 seg13 (mission_03) |
| L==10 | 910 экз. (все g[0]=5), x:[-112,2626], y:[1,1524] px |
| враги | 448 (state -1: 305, 0: 84, 1: 59) |
| NPC | 31 |

## 4. L==10 и opcode 147

- g.z (скриптовый массив): 6078 байт; **147 на позиции 6028** (1 шт); 146: 22 позиции; 148: 2.
- Слова 147-скрипта в g.z совпадают со статикой: [6,2,2,6,1489,1519,35,1,65,18,98,59,99,55] (LE u16).
- Тайл-ячейка 31: tile_id=1527, x=4, y=0 (данные, offset 27218); скрипт 147 прикреплён к тайлу.
- Скрипт миссии реально выполняется: bO 1176→1444, bQ 20→0 (FIRE продвигает).

## 5. Mission object (i/cB/Q[])

- **i=0, cB=0, cy=0, cz=-1, Q=[0,0,0,0,0,0,0,0], cU=0, cW=0, cY=0** — активный объект НЕ установлен.
- Q[] структура (8 int = 4 пары) подтверждена в рантайме.
- Попытки активации: подход к тайлу 31 (координаты (32,112) и (64,0)), подход к двери (208,352) + FIRE, диалог с ближайшим NPC (75,456) + FIRE, широкий обход (запад→север). Триггер не сработал; у NPC — диалоговая петля (телепорт игрока).

## 6. cB / damage / completion

- cB ни разу не наблюдался >0; событий урона нет; completion НЕ достигнут (state 7 не появлялся).
- По ТЗ: не считать hypothesis подтверждённой/опровергнутой — INCONCLUSIVE.

## 7. Реальные кадры (evidence/real/test-003/)

- `mission-start.png` — кадр после загрузки mission_03 (state 6, bz=1)
- `mission-object.png` — кадр в зоне старта (объекты L10)
- Файлы objective-active/completion/results/shop **не созданы** (экраны не были реально показаны — по ТЗ fake не создаём).

## 8. Сравнение со статикой (comparison.json, 13 пунктов)

MATCH ×6 (миссия загружена; механика загрузки; L10 существуют; opcode 147 и слова; скриптовый движок bO/bQ; карта), INCONCLUSIVE ×5 (активация MO; cB initial; cB damage; completion; reward), PARTIAL ×2 (Q[] структура — значения 0; save — только профиль). MISMATCH ×0.

## 9. Аудио

**AUDIO_RUNTIME_BLOCKED_BY_STUB** — в PASS/FAIL не входит.

## 10. Ограничения

1. Триггер тайла 31 недостижим в FreeJ2ME (стены/диалоговая петля) — не удалось активировать объект.
2. Возможно, объект активируется после сюжетной цепочки, требующей игровых действий, невозможных в эмуляторе.
3. Для полного прохода нужен другой эмулятор (j2me-loader и т.п.), недоступный в песочнице.

## 11. Knowledge updates

MASTER_FACTS.json: добавлены F049–F051 (mission 3 runtime, загрузка, 147 в g.z). runtime-frontier-v4.json: test003.
