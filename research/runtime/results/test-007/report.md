# TEST-007 — REAL RUNTIME SHOP / ECONOMY / WEAPON PRICES — ОТЧЁТ

**Дата:** 2026-08-22
**Тест:** TEST-007 (Этап 15.36)
**Runtime:** FreeJ2ME SDL (Anbu) x86 JRE 17; JAR SHA-256 `1111f12b...` (не изменён)
**Миссия:** mission 03 (RUNTIME_ASSISTED bB=3)
**Вход в shop:** RUNTIME_ASSISTED (JDWP d(13,0); экономические поля НЕ изменялись; au[0]=1 — UI-стейт магазина)

## 1. Итог

**PARTIAL** — state 13 SHOP открыт, **L[4]=45000 (валюта) ПОДТВЕРЖДЕНА**, L[0]=активное оружие, экран shop реально отображается (shop.png). Покупка/цены НЕ воспроизведены: реальный вход через L14-объект не сработал (5 ближайших объектов из 62), assisted-вход не инициализирует UI-цепочку товаров (g.D()/C[]).

## 2. SHOP ENTRY SNAPSHOT (shop-entry.json)

| Поле | Значение |
|---|---|
| state / sub | 13 / 0 |
| L[] | **[-26, 0, 0, 0, 45000]** |
| L[4] | **45000** (cap/init) |
| L[0] | -26 (активное оружие; -24 в предыдущем прогоне — меняется) |
| y (апгрейды [[I) | [[-1,-1,0,0,0],[-1,-1,0,0,2],[-1,-1,0,2,7],[35,104,0,9,1],[101,123,1,10,1],...] — 8 строк × 5 |
| b[][] | b[0]={1,-1,3,13,45,8,-2,4,5,6,7,1,-1} — таблица МЕНЮ (совпадает со статикой) |
| au[] / as[] / at[] | [0×13] / [-1,0,...] / [0×4] |
| bt | 2 |

## 3. Currency (currency-runtime.json / L[])

**L[4]=45000 — валюта подтверждена** (MATCH со статикой: init 45000, cap 45000). L[0] = активное оружие (NEW_OBSERVATION: в статике L[1]). L[2]=0 (cap 2800 не проверен).

## 4. Weapon inventory

L[0]=-26 (ID оружия). 16 слотов НЕ наблюдались (нет списка в UI). y-таблица = 8 строк (апгрейды). rank (b[n][15]) НЕ подтверждён — g.b = таблица меню, не ранги.

## 5. Prices

**UNKNOWN** — цены не отображаются (UI товаров не инициализирован при assisted входе). Связка runtime_price→static_record не построена.

## 6. Purchase test

FIRE/DOWN/UP ×60с: L[] не изменился. Покупка НЕ воспроизведена (INCONCLUSIVE). Причина: fE() case 1→2 требует g.D() (C[0]!=-1, диалог) — не инициализировано.

## 7. Denied purchase

Не тестировалось (нет доступного товара для покупки).

## 8. Shop exit

НЕ наблюдался (state 13 стабилен; прогон остановлен по таймеру).

## 9. Save

RMS не менялся (покупки не было) — отсутствие write не доказательство отсутствия persistence (по ТЗ).

## 10. Скриншоты (evidence/real/test-007/)

shop.png (state 13, текстуры меню). weapon-list/purchase-before/purchase-after/shop-exit — НЕ созданы (экраны не показывались реально).

## 11. Сравнение со статикой (comparison.json, 13 пунктов)

MATCH ×2 (L[4]=45000; cap 45000), PARTIAL ×3 (state 13 вход; y структура; b[][]), NEW_OBSERVATION ×1 (L[0]=weapon), INCONCLUSIVE ×7 (L[2] cap; 16 слотов; цены; покупка; выход; RMS; скидка). MISMATCH ×0.

## 12. Аудио

AUDIO_RUNTIME_BLOCKED_BY_STUB.

## 13. Ограничения

- Реальный вход в shop (L14 объект) не воспроизведён: 62 объекта, 5 ближайших не сработали; вероятно, нужен конкретный объект-магазин.
- UI-цепочка товаров (g.D()/C[]) требует реального входа — assisted переход не инициализирует её.
- Ранги оружия не в g.b (это меню) — поле ранга в другом месте (не найдено в runtime).

## 14. Knowledge updates

MASTER_FACTS: L[4]=45000 runtime подтверждён; L[0]=weapon (NEW); покупка/цены остаются REQUIRES_RUNTIME.
