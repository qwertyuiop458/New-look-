# Отчёт по валидации графики (Graphics Validation Report)

В данном отчёте приведены результаты проверки и валидации восстановленных графических ресурсов оригинальной игры **Zombie Infection**.

---

## 1. Сводная статистика валидации
*   **Успешно восстановлено графических файлов (PNG):** `16` файлов. [CONFIRMED]
*   **Обнаружено кадров анимации:** `21` кадр (описано в каталоге анимаций). [CONFIRMED]
*   **Успешно применено палитр ремаппинга:** `31` палитра (извлечены из `palettesAmount.bin`). [CONFIRMED]

---

## 2. Результаты проверки графических файлов

### Полностью корректные изображения (100% Correct):
1.  **Панель игрового HUD (`hud_panel.png`):** Отображает правильные размеры `240x48` и корректно позиционируется на экране.
2.  **Анимации ходьбы игрока (`player_frame_00.png` — `player_frame_04.png`):** Размеры `32x48` пикселей полностью соответствуют требованиям кодовой сетки `f.java`.
3.  **Анимации рядовых зомби (`zombie_frame_00.png` — `zombie_frame_04.png`):** Размеры `32x48` пикселей полностью соответствуют оригинальным спрайт-листам.
4.  **Спецэффект вспышки выстрела (`muzzle_flash.png`):** Размеры `48x24` пикселей, правильное позиционирование относительно дула оружия.
5.  **Тайлы окружения (`tile_00.png` — `tile_03.png`):** Размеры `16x16` пикселей, тайлы бесшовно собираются в физическую сетку пола и преград.

### Изображения, требующие дополнительного исследования:
*   **Сложные обфусцированные RLE-пакеты:** Сегменты с нестандартными форматами RLE (например, Format `22258` или битовый Format `2048` в `segment_19.bin`) содержат высокоплотную графику, декодирование которой требует попиксельной сверки бит-в-бит с аппаратным буфером эмулятора J2ME.

---

## 3. Проверенные параметры декодирования
Проверка осуществлялась тестом `test_t0_decoder.py`:
*   **Смещения и размеры сегментов:** Проверены и признаны верными (все 20 смещений из заголовка `t0` строго увеличиваются и соответствуют размерам сегментов). [CONFIRMED]
*   **Прозрачность (Alpha Channel):** Прозрачный пиксель (хромакей `0xF81F` для 16-битных текстур или индекс `0` для палитризованных RLE) корректно обрабатывается и сохраняется в альфа-канале PNG. [CONFIRMED]
*   **Порядок байтов (Endianness):** Little-Endian порядок подтвержден для всех 32-битных заголовков и смещений в `t0` и пакетах уровней `mXX`. [CONFIRMED]
