# V3 Remaining Static Frontier — что ещё можно и нельзя решить статически

**Дата:** 2026-08-15

## 1. Вопросы, которые МОЖНО решить анализом кода/бинарных данных (не закрыты)

| Вопрос | Что нужно | Сложность |
| :--- | :--- | :--- |
| Вызов `g.a(f2, n, byArray, n2)` (создание объекта-миссии) | повторный анализ CFR-обфусцированного участка / поиск по сигнатуре аргументов | средняя |
| Семантика prop13/16/18 | изоляция контекстов чтений в f.java (строки ~2830) | низкая |
| Q[] значения для 5 миссий | поиск 8-байтовых блоков в миссиях (пары u16) по паттерну | средняя |
| Число слотов сейвов | анализ g.C() (профили) и B(int) вызовов | средняя |
| Расшифровка всех 19 опкодов сценарного парсера a(byte[]) (1682) | глубокий разбор веток (не путать с 46 mission opcode) | высокая |
| Что хранится в m8 seg0/1/2 (объекты) — точные роли type 10/14/15/18/22/23 | сопоставление с рендером/коллизиями | высокая |
| prop21 vs prop31 (палитра vs звук) | все чтения обоих пропов | низкая |

## 2. Вопросы, которые ПРИНЦИПИАЛЬНО требуют запуска оригинала

- Выбор следующей миссии (bB) и переход через shop;
- Источник урона cB (кто бьёт объект-миссию);
- Числовые пары X-индекс → объект спрайт-листа;
- Реальные FPS/RAM/GC;
- Аудио: какая MIDI = какая музыка; фактическое звучание;
- Агро-единицы 1600/4096;
- Failure condition (state 10 trigger);
- Race conditions при cB=0/смерть/cy;
- Цены магазина; тип наград k(n,10);
- Точный UI сейвов.

## Вывод

Статический анализ близок к исчерпанию высокоценных направлений.
Дальнейшие значимые открытия — только через запуск оригинала (эмулятор J2ME).
